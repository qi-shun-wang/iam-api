# Build Up Project

## Local Docker

     git clone ....
     cd ./fd-iam
     cp .env.local.example .env

### Run Up Remote With Proxy Network Docker Stack

     docker-compose up -d
### To Rebuild Image     
     docker-compose up --build
